all:
  children:
    master:
      hosts:
        master:
          ansible_host: master
          ansible_user: root

    workers:
      hosts:
        worker1:
          ansible_host: worker1
          ansible_user: root
        worker2:
          ansible_host: worker2
          ansible_user: root